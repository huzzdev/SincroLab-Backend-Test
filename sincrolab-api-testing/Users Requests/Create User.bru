meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{host}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "newuser@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
